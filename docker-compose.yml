version: "3.9"

services:
  server:
    image: casper9600/my-app-server:latest
    ports:
      - "5000:5000"
    restart: unless-stopped

  client:
    image: casper9600/my-app-client:latest
    ports:
      - "3000:3000"
    restart: unless-stopped
